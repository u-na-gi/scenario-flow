
services:
  sample-api1:
    build:
      context: ./sample-api1
      dockerfile: dockerfile
    ports:
      - "3323:3323"
    container_name: sample-api1
    tty: true
    volumes:
      - ./sample-api1:/app
    command: >
      tail -f /dev/null

  sample-api2:
    build:
      context: ./sample-api2
      dockerfile: dockerfile
    ports:
      - "3324:3323"
    container_name: sample-api2
    # command: >
    #   sh -c "deno run --allow-net main.ts"
